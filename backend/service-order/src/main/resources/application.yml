spring:
  application:
    name: service-order  # Eureka


  datasource:
    url: jdbc:postgresql://ep-gentle-flower-a816ozgy-pooler.eastus2.azure.neon.tech/service-order?sslmode=require&channel_binding=require
#    localhost:5432/service-order
    username: service-order_owner
    password: npg_VgD8lmH9LQOy
    driver-class-name: org.postgresql.Driver

  h2:
    console:
      enabled: true  # 启用 H2 控制台，默认访问路径为 /h2-console

  jpa:
    hibernate:
      ddl-auto: update  # 自动更新数据库模式（可以设置为 'create', 'validate', 'none' 等）
    show-sql: true  # 显示 SQL 查询语句

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # Eureka
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
    hostname: ${spring.cloud.client.ip-address}

server:
  port: 8018