spring:
  application:
    name: service-user  # Eureka


  datasource:
      url: jdbc:postgresql://ep-lucky-cake-a8jchw20-pooler.eastus2.azure.neon.tech/service-user?sslmode=require&channel_binding=require
#    localhost:5432/service-user
      username: service-user_owner
      password: npg_FJAN6WaS3ZXK
      driver-class-name: org.postgresql.Driver

  h2:
    console:
      enabled: true  # 启用 H2 控制台，默认访问路径为 /h2-console

  jpa:
    hibernate:
      ddl-auto: update  # 自动更新数据库模式（可以设置为 'create', 'validate', 'none' 等）
    show-sql: true  # 显示 SQL 查询语句
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # Eureka
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
    hostname: ${spring.cloud.client.ip-address}

server:
  port: 8016