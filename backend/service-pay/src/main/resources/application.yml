spring:
  application:
    name: service-pay  # Eureka


  datasource:
    url: jdbc:h2:mem:testdb  # ???????
    driverClassName: org.h2.Driver
    username: sa
    password: password

  h2:
    console:
      enabled: true  # ?? H2 ??????????? /h2-console

  jpa:
    hibernate:
      ddl-auto: update  # ??????????????? 'create', 'validate', 'none' ??
    show-sql: true  # ?? SQL ????

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # Eureka

server:
  port: 8019